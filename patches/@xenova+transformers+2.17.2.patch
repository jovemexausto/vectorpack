--- a/node_modules/@xenova/transformers/src/utils/image.js
+++ b/node_modules/@xenova/transformers/src/utils/image.js
@@ -13,7 +13,13 @@
 import { Tensor } from './tensor.js';
 
 // Will be empty (or not used) if running in browser or web-worker
-import sharp from 'sharp';
+let sharp;
+try {
+    // Optional dependency; avoid hard crash for text-only use cases.
+    sharp = (await import('sharp')).default;
+} catch {
+    sharp = null;
+}
 
 const BROWSER_ENV = typeof self !== 'undefined';
 const WEBWORKER_ENV = BROWSER_ENV && self.constructor.name === 'DedicatedWorkerGlobalScope';
@@ -51,7 +57,10 @@
     }
 
 } else {
-    throw new Error('Unable to load image processing library.');
+    // Defer failure until image APIs are actually used.
+    loadImageFunction = async () => {
+        throw new Error('Image processing unavailable (sharp not installed).');
+    };
 }
 
 

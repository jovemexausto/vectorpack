# Culture memo

We build like maintainers, not like a growth team.

That means:
- no hidden behaviors
- stable specs over fancy demos
- deterministic builds over shiny features

We choose boring defaults and document the edge cases.
